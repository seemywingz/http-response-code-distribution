HTTP Response Code Distribution
================================


### Usage

* `git clone https://github.com/seemywingz/http-response-code-distribution.git`
* `cd http-response-code-distribution`
* you can run the script by itself `./http_dist` or supply an nginx config `./http_dist /var/log/my_nginx.log`






###### Kevin Jayne
